if [[ -f /etc/lsb-release ]]; then
    source /etc/lsb-release
else
    echo "Unknown"
    exit 1
fi

if [[ "$DISTRIB_ID" != "Ubuntu" ]]; then
    echo "Unknown"
    exit 1
elif [[ -f /var/run/reboot-required ]]; then
    echo "Yes: reboot required"
else
    echo "No: reboot not required"
fi
